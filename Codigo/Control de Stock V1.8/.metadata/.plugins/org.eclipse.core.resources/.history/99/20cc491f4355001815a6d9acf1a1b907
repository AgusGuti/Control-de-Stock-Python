from ClaseProducto import *
from dicDatos import *
from dicCafe import *
from manejoDeArchivos import *
from funciones import *
from operator import invert
from _overlapped import NULL
from menu import mostrarMenuPromo
from dicPromo import dicPromo

salir =False

contador1=0
contador2=0
contador3=0
contador4=0
contador5=0
contador6=0


while(salir == False):
	limpia()
	
	mostrarMenuPromo()

	opcion = leerEntero("Ingrese el item de cafe/promo: ")
	print()
	cantidad = leerEntero("Ingrese la cantidad vendida: ")
	print()
	dicPromo[opcion].cntVentas += 1

	if (opcion==1) :
		
		

	pass
	
	if (opcion==2) :
		cantidad = leerEntero("Ingrese la cantidad de promociones: ")
		contador2=contador2+cantidad
	pass

	if (opcion==3) :
		cantidad = leerEntero("Ingrese la cantidad de promociones: ")
		contador3=contador3+cantidad
	pass

	if (opcion==4) :
		cantidad = leerEntero("Ingrese la cantidad de promociones: ")
		contador4=contador4+cantidad
	pass

	if (opcion==5) :
		cantidad = leerEntero("Ingrese la cantidad de promociones: ")
		contador5=contador5+cantidad
	pass

	if (opcion==6) :
		cantidad = leerEntero("Ingrese la cantidad de promociones: ")
		contador6=contador6+cantidad
	pass

pass